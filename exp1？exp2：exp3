int main()
{
	int a = 10;
	int b = 20;
	int max = 0;
	if (a > b)
		max = a;
	else
		max = b;
	max = (a > b ? a : b);
	//exp1 ? exp2 : exp3  表达式1为真，执行表达式2，否则执行表达式3
	/*int a = 0;
	int b = 0;
	if (a > 5)
		b = 3;
	else
		b = -3;
	b = (a > 5 ? 3 : -3);*/

	return 0;
}
