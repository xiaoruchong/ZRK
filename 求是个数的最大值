int main()
{
	int i = 0;
	int arr[] = { 1,2,3,4,5,6,7,8,9,0 };
	int max = arr[0];//max必须从数组里选一个避免给max赋值0导致0为最大值的可能；
	int sz = sizeof(arr) / sizeof(arr[0]);
	for (i = 0; i <= sz; i++)
	{
		if (arr[i] > max)
		{
			max = arr[i];
		}
	}
	printf("max = %d\n", max);
	return 0;
}
